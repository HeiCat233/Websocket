"""
================================================================================
Web服务器主入口
================================================================================

【程序说明】
Python实现的简易Web服务器，基于Socket API从零构建。

【功能特性】
- HTTP/1.0协议支持
- 多线程并发处理
- 静态文件服务
- 路径安全检查
- 优雅关闭

【使用方式】
```bash
# 启动服务器
python src/main.py

# 或（需要在src目录下）
cd src
python main.py
```

【环境变量】
- SERVER_HOST: 监听地址（默认0.0.0.0）
- SERVER_PORT: 监听端口（默认8080）
- DOCUMENT_ROOT: 文档根目录
- THREAD_POOL_SIZE: 工作线程数（默认4）

================================================================================
"""

from .server import create_server


def main():
    """
    主函数
    """
    server = create_server()
    server.start()


if __name__ == '__main__':
    main()
